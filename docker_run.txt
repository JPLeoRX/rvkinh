# Build
docker build -t tekleo-security-image .

# Run (local)
docker run --name=tekleo-security --rm -ti -p 9543:9543 tekleo-security-image

# Run (local one command)
docker build -t tekleo-security-image . && docker run --name=tekleo-security --rm -ti -p 9543:9543 tekleo-security-image